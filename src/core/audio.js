// core/audio.js
// Placeholder audio (per ora solo stub)

const AudioSys = (() => {
  function play(name) {
    // In futuro: suoni veri
    // console.log("Audio:", name);
  }
  function toggleMute() {
    // per ora nulla
  }
  return {
    play,
    toggleMute
  };
})();
